[project]
name = "splineintersections"
version = "0.1.0"
description = "Code for finding intersections between 3D splines"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "matplotlib>=3.11.0",
    "numpy>=2.5.1",
    "seaborn>=0.13.2",
]
